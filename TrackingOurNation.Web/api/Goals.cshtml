@{
    //var filter = UrlData[1];
    //if (filter != null)
    //{
    var db = Database.Open("DataDB");
    var sql = "SELECT DISTINCT GoalName, GoalDescription FROM GoalsDetails";
    var data = db.Query(sql);

    var json = Json.Encode(data);
    Response.Write(json);
    //}else{
    //    Response.StatusCode = 404;
    //    var json = "Not Found";
    //    Response.Write(json);
    //}

}

